import React from 'react'

const SchemePromptModal = ({ description }) => {

    return (
        <div className={`scheme-prompt__modal turn-off`}>
            {description}
        </div>
    )
}

export default SchemePromptModal